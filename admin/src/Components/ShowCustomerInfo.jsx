import React from 'react'

const ShowCustomerInfo = ({showInfo}) => {
  return (
    <div className={`${showInfo ? "block" : "hidden"} absolute inset-0 w-full h-full`} >
        

    </div>
  )
}

export default ShowCustomerInfo